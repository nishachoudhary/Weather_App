const initialState = {
    zipCode: '201309',
   
};

export default (state = initialState, action) => {
    switch(action.type){
       /* case UPDATE_ZIP:{
            return {
                ...state,
                zipCode: action.data
            }
        }*/
    }
    return state;
};